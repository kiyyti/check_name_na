# เปิดใช้งานหน้าแอดมิน

โค้ดรองรับบัญชีแอดมินเดียว ไม่มีระบบสมัครสมาชิก บัญชีที่ตั้งให้ใช้ชื่อผู้ใช้ `admin_checknamena123` และรหัสผ่านตามที่เจ้าของระบุ รหัสผ่านถูกแฮชด้วย scrypt ไม่ได้เก็บรหัสผ่านจริงใน source code

## 1. อัปเดต Google Apps Script ก่อน

1. คัดลอกโค้ดทั้งหมดจาก `google-apps-script/Code.gs` ไปแทนที่ใน Apps Script แล้วบันทึก
2. ไปที่ Deploy → Manage deployments → เลือก deployment เดิม → Edit → New version → Deploy
3. ใช้ URL `/exec` เดิม และ `SPREADSHEET_ID` / `API_SECRET` เดิม
4. เก็บ trigger `finalizeAbsences` เดิมไว้ (ทุก 5 นาที)

ไม่ต้องสร้างชีตใหม่ แท็บ Settings, Students, Sessions, Attendance, AuditLog ใช้ชุดเดิม เมื่อบันทึกรอบผ่านแอดมินครั้งแรก ระบบจะเพิ่มคอลัมน์ `late_at`, `absence_at`, `room_name`, `latitude`, `longitude`, `radius_m`, `max_accuracy_m` ต่อท้าย Sessions อัตโนมัติ

รอบเดิมที่ยังไม่ได้กำหนดค่าเหล่านี้จะใช้ Settings เป็นค่าเริ่มต้น รอบใหม่จะบันทึกค่าแยกต่อรอบ ไม่เปลี่ยนตาม Settings ในภายหลัง

## 2. เพิ่มค่าบัญชีใน Vercel

ไฟล์ส่วนตัว `web/.env.admin.vercel` เตรียมค่า 3 ตัวแล้ว:

- `ADMIN_USERNAME`
- `ADMIN_PASSWORD_HASH`
- `ADMIN_SESSION_SECRET`

เปิดไฟล์นี้ในเครื่องแล้วคัดลอกค่าทั้ง 3 ตัวลง Vercel → Project → Settings → Environment Variables สำหรับ Production (และ Preview ถ้าจะทดสอบ Preview) หรือใช้ Import .env ถ้ามีปุ่มนี้

อย่าใส่ `NEXT_PUBLIC_` และอย่าคัดลอกรหัสผ่านจริงลงช่อง `ADMIN_PASSWORD_HASH` ค่านั้นต้องใช้ข้อความแฮชจากไฟล์เท่านั้น ไม่ต้องแก้ค่า GOOGLE_APPS_SCRIPT_URL / GOOGLE_APPS_SCRIPT_SECRET

ไฟล์ `.env` และ `.env.admin.vercel` ถูกกันออกจาก Git และ Vercel source upload เก็บไฟล์ส่วนตัวนี้ไว้ในเครื่อง ไม่ส่งในแชตหรือ commit

## 3. ส่งโค้ดขึ้น GitHub

จากโฟลเดอร์ checkname:

```bash
git add web google-apps-script/Code.gs google-apps-script/logic.test.mjs tools/setup-admin.mjs tools/admin-http.test.mjs tools/admin-http-gateway.mock.mjs ADMIN-SETUP.md README.md
git diff --cached --name-only
git commit -m "Add single-account admin dashboard"
git push origin main
```

หลัง Vercel deploy สำเร็จ เปิด `https://โดเมนของคุณ/admin` แล้วล็อกอิน ถ้าแก้ Environment Variables หลัง deploy แล้ว ให้ Redeploy อีกครั้ง

## การใช้งาน

- ภาพรวม: เลือกรอบ ดูจำนวนแต่ละสถานะ ค้นหานักเรียน แก้ผลพร้อมเหตุผล
- นักเรียน: เพิ่ม/แก้ไขชื่อ รหัส ระดับ กลุ่ม วันที่เริ่มและพ้นสภาพ ใช้ inactive แทนการลบประวัติ
- รอบเรียน: กำหนดเวลาไทยและสถานที่เอง เปิด/ปิด/ยกเลิกรอบ คัดลอกไปสัปดาห์ถัดไป หรือสร้างรอบทุกวัน/ทุกสัปดาห์ล่วงหน้าสูงสุด 12 รอบ
- ระดับชั้นของรอบเรียน: ติ๊กได้หลายระดับ เช่น ปวช.1 และ ปวช.3 หรือเลือก “ทุกระดับชั้น” ชัดเจน หากยกเลิกทุกตัวเลือกจะบันทึกไม่ได้ ต้องเลือกอย่างน้อยหนึ่งชั้นหรือเลือกทุกระดับชั้น รายการมีระดับอาชีวะพื้นฐานและระดับชั้นจากข้อมูลเดิม
- ตั้งค่า: ข้อมูลวิชาและค่าเริ่มต้นสำหรับรอบใหม่
- ประวัติ: แสดง 50 การแก้ไขล่าสุด ประวัติทั้งหมดอยู่ใน AuditLog การแก้โดยตรงใน Google Sheets ไม่อยู่ในประวัติของแอดมิน

เวลาแต่ละรอบต้องเรียง: เปิดเช็คชื่อ ≤ เริ่มเรียน ≤ สาย < ขาด ≤ ปิดเช็คชื่อและจบเรียน ทุกเวลาเป็นวันเดียวกัน กลุ่มที่อาจมีผู้เรียนคนเดียวกันต้องไม่เปิดเช็คชื่อซ้อนกัน

การเลือกหลายระดับชั้นต้องอัปเดต Code.gs และ deployment ของ Apps Script ก่อนใช้งาน เว็บจะตรวจว่ารองรับแล้วก่อนบันทึก ระบบใช้คอลัมน์ Sessions.class_level เดิม เก็บหลายค่าเป็นข้อความคั่นด้วยจุลภาค เช่น `ปวช.1, ปวช.3` ส่วนค่าว่างยังหมายถึงทุกระดับชั้น ข้อมูลเดิมที่มีชั้นเดียวใช้ได้เหมือนเดิม ไม่ต้องเพิ่มคอลัมน์ และ Students.class_level ยังคงเป็นหนึ่งระดับต่อผู้เรียน

การสร้างหลายรอบจะบันทึกทีละรอบ หากบางรอบไม่ผ่าน ระบบจะแจ้งจำนวนที่สร้างสำเร็จและหยุด ให้ตรวจรายการล่าสุดก่อนสร้างเฉพาะส่วนที่เหลือ

เซสชันล็อกอินหมดอายุใน 8 ชั่วโมง ทุก API ตรวจสิทธิ์ฝั่งเซิร์ฟเวอร์ และคำขอแก้ไขตรวจ Origin ประวัติระบุบัญชีเดียวกันทุกครั้งเพราะใช้บัญชีร่วมกัน การล็อกอินจำกัด 8 ครั้งต่อ IP ในหน้าต่าง 15 นาที และรวม 60 ครั้งผ่าน Script Properties พร้อม lock (บัญชีที่สำเร็จก็นับเป็นหนึ่งครั้ง)

## ทดสอบหลัง deploy

1. เปิด /admin ในหน้าต่างไม่ระบุตัวตน ต้องเข้าสู่หน้าล็อกอิน และ /api/admin/data ต้องตอบ 401
2. ล็อกอินด้วยบัญชีที่กำหนด ตรวจว่าดึงรายชื่อจริงได้
3. ใช้นักเรียนทดสอบ เพิ่มรอบแบบร่าง แก้ไขและคัดลอกหนึ่งครั้ง
4. เปิดรอบทดสอบ ตรวจการเช็คชื่อจากมือถือ และตรวจผลใน Attendance
5. แก้ผลจากหน้าแอดมินพร้อมเหตุผล ตรวจ AuditLog และเช็คซ้ำว่าผลที่อาจารย์แก้ไม่ถูกเขียนทับ
6. ออกจากระบบ แล้วตรวจว่าเรียก /api/admin/data ไม่ได้

การทดสอบในเครื่องใช้ข้อมูลจำลอง ไม่ได้เปลี่ยน Google Sheets จริงหรือพิสูจน์โหลดนักเรียน 300 คนพร้อมกัน

แนวทาง auth: https://nextjs.org/docs/app/guides/authentication
